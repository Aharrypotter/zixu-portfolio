# Zixu Portfolio

Personal website: AI compilers, GPU kernels, and inference systems.

Public site files are in `public/`. Deploy with `npm ci && npm run deploy`. Cloudflare authentication is required. No private workspace files belong in this repository.
